# MCP Mail macOS v1.1 — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Ajouter au MCP Mail macOS le multi-comptes à l'envoi, les outils répondre/transférer, le CC/CCI, et une suite de tests automatisés, en restant 100 % rétrocompatible.

**Architecture:** Refactor en couches : extraire la génération des scripts AppleScript dans un module pur `scripts.ts` (testable sans Mail), `mail.ts` orchestre (exécute + parse + valide l'expéditeur), `index.ts` déclare les outils MCP. Tests via `node:test` sur la sortie compilée.

**Tech Stack:** Node.js 24, TypeScript (NodeNext), `@modelcontextprotocol/sdk`, `zod`, AppleScript via `osascript`, `node:test` intégré.

## Global Constraints

- Cible : **macOS uniquement** (`compatibility.platforms: ["darwin"]`).
- **Aucune dépendance npm ajoutée** (tests avec `node:test` intégré).
- **Zéro action irréversible** ; `repondre_mail`/`transferer_mail` créent un **brouillon par défaut** (`envoyer: false`).
- **Rétrocompatibilité** : les nouveaux paramètres (`cc`, `cci`, `expediteur`) sont tous optionnels ; aucun comportement v1.0 ne change quand ils sont absents.
- Imports internes TypeScript en **`.js`** (NodeNext). Tests importent la sortie compilée (`../dist/*.js`).
- Messages utilisateur en **français**, sans tirets longs. Version cible : **`1.1.0`**.
- Tous les littéraux de chaîne insérés dans AppleScript passent par `asStr()` (échappement).

## Fichiers

- Créer : `src/scripts.ts` — builders AppleScript purs + helpers (`recipientsBlock`, `extraRecipientsBlock`, `senderLine`, `FIND_MESSAGE_HANDLER`).
- Modifier : `src/mail.ts` — orchestration, `listerComptes`, `resolveExpediteur`, exports testables.
- Modifier : `src/index.ts` — 2 nouveaux outils + params `cc`/`cci`/`expediteur`.
- Modifier : `src/applescript.ts` — (inchangé fonctionnellement ; déjà exporte `asStr`, `MailError`, séparateurs).
- Créer : `test/scripts.test.ts`, `test/parsing.test.ts`, `test/expediteur.test.ts`.
- Modifier : `package.json` (version, script `test`), `manifest.json` (10 outils, version), `README.md`.

## Types partagés (référence pour toutes les tâches)

```ts
// mail.ts
export interface ResumeMail { id: number; expediteur: string; sujet: string; date: string; lu: boolean; }
export interface MailComplet extends ResumeMail { corps: string; }
export interface Compte { nom: string; emails: string[]; }
```

```ts
// scripts.ts — signatures produites
export const FIND_MESSAGE_HANDLER: string;
export function recipientLines(adresses: string, kind: "to" | "cc" | "bcc"): string;
export function recipientsBlock(opts: { to: string; cc?: string; cci?: string }): string; // throws MailError si aucun "to"
export function extraRecipientsBlock(cc?: string, cci?: string): string;
export function senderLine(adresse?: string): string;
export function buildListerScript(o: { filtre: "tous" | "non_lus"; expediteur?: string; limite: number }): string;
export function buildRechercherScript(o: { motCle: string; expediteur?: string; depuisJours?: number; inclureCorps: boolean; limite: number }): string;
export function buildLireScript(id: number): string;
export function buildListerComptesScript(): string;
export function buildComposeScript(o: { destinataire: string; sujet: string; corps: string; cc?: string; cci?: string; expediteur?: string; action: "save" | "send" }): string;
export function buildRepondreScript(o: { id: number; corps: string; repondreATous: boolean; cc?: string; cci?: string; expediteur?: string; action: "save" | "send" }): string;
export function buildTransfererScript(o: { id: number; destinataire: string; corps?: string; cc?: string; cci?: string; expediteur?: string; action: "save" | "send" }): string;
export function buildMarquerScript(id: number, lu: boolean): string;
export function buildDeplacerScript(id: number, dossier: string): string;
export function buildCorbeilleScript(id: number): string;
```

---

### Task 1: Harness de test + extraction des builders existants dans `scripts.ts`

Refactor sans changement de comportement : déplacer la génération des scripts AppleScript (aujourd'hui inline dans `mail.ts`) vers un module pur `scripts.ts`, et brancher `mail.ts` dessus. Établit `npm test`.

**Files:**
- Create: `src/scripts.ts`
- Modify: `src/mail.ts`
- Modify: `package.json`
- Test: `test/scripts.test.ts`, `test/parsing.test.ts`

**Interfaces:**
- Consumes: `asStr`, `MailError`, `FIELD_SEP`, `RECORD_SEP` depuis `applescript.js`.
- Produces: tous les `build*` listés ci-dessus pour les 8 ops existantes + `recipientLines`/`recipientsBlock`/`extraRecipientsBlock`/`senderLine`/`FIND_MESSAGE_HANDLER` ; `mail.ts` ré-exporte `parseResumes` (pour tests).

- [ ] **Step 1: Ajouter le script de test dans `package.json`**

Dans `package.json`, remplacer le bloc `scripts` par :

```json
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "test": "npm run build && node --test test/*.test.ts"
  },
```

- [ ] **Step 2: Écrire les tests des builders (échec attendu)**

Créer `test/scripts.test.ts` :

```ts
import test from "node:test";
import assert from "node:assert";
import {
  buildListerScript,
  buildRechercherScript,
  buildLireScript,
  recipientsBlock,
} from "../dist/scripts.js";

test("buildListerScript: filtre tous n'ajoute pas de clause whose", () => {
  const s = buildListerScript({ filtre: "tous", limite: 10 });
  assert.ok(s.includes("messages of inbox"));
  assert.ok(!s.includes("whose"));
  assert.ok(s.includes("set maxN to 10"));
});

test("buildListerScript: non_lus ajoute read status is false", () => {
  const s = buildListerScript({ filtre: "non_lus", limite: 5 });
  assert.ok(s.includes("read status is false"));
});

test("buildListerScript: expediteur ajoute sender contains", () => {
  const s = buildListerScript({ filtre: "tous", expediteur: "bob@x.com", limite: 3 });
  assert.ok(s.includes('sender contains "bob@x.com"'));
});

test("buildRechercherScript: inclureCorps ajoute content of m contains", () => {
  const s = buildRechercherScript({ motCle: "facture", inclureCorps: true, limite: 10 });
  assert.ok(s.includes('subject of m contains "facture"'));
  assert.ok(s.includes("content of m contains"));
});

test("buildRechercherScript: sans corps ne cherche pas dans le contenu", () => {
  const s = buildRechercherScript({ motCle: "x", inclureCorps: false, limite: 10 });
  assert.ok(!s.includes("content of m contains"));
});

test("buildLireScript: amorce la concaténation avec une chaîne vide", () => {
  const s = buildLireScript(42);
  assert.ok(s.includes("findMessage(42)"));
  assert.ok(s.includes('"" & (id of m)'));
});

test("recipientsBlock: un destinataire", () => {
  const b = recipientsBlock({ to: "a@x.com" });
  assert.ok(b.includes('{address:"a@x.com"}'));
});

test("recipientsBlock: plusieurs destinataires", () => {
  const b = recipientsBlock({ to: "a@x.com, b@y.com" });
  assert.ok(b.includes('{address:"a@x.com"}'));
  assert.ok(b.includes('{address:"b@y.com"}'));
});

test("recipientsBlock: aucun destinataire lève une erreur", () => {
  assert.throws(() => recipientsBlock({ to: "   " }));
});
```

Créer `test/parsing.test.ts` :

```ts
import test from "node:test";
import assert from "node:assert";
import { parseResumes } from "../dist/mail.js";
import { FIELD_SEP, RECORD_SEP } from "../dist/applescript.js";

test("parseResumes: enregistrement simple", () => {
  const raw = ["27904", "Bob <b@x.com>", "Sujet", "hier", "false"].join(FIELD_SEP) + RECORD_SEP;
  const r = parseResumes(raw);
  assert.equal(r.length, 1);
  assert.deepEqual(r[0], { id: 27904, expediteur: "Bob <b@x.com>", sujet: "Sujet", date: "hier", lu: false });
});

test("parseResumes: chaîne vide -> tableau vide", () => {
  assert.deepEqual(parseResumes(""), []);
});

test("parseResumes: lu=true correctement interprété", () => {
  const raw = ["1", "A", "S", "d", "true"].join(FIELD_SEP) + RECORD_SEP;
  assert.equal(parseResumes(raw)[0].lu, true);
});
```

- [ ] **Step 3: Lancer les tests pour vérifier l'échec**

Run: `npm test`
Expected: FAIL — `Cannot find module '../dist/scripts.js'` (le module n'existe pas encore) et/ou `parseResumes` non exporté.

- [ ] **Step 4: Créer `src/scripts.ts` avec les 8 builders existants + helpers**

```ts
import { asStr, MailError } from "./applescript.js";

export const FIND_MESSAGE_HANDLER = `
on findMessage(theId)
  tell application "Mail"
    set hits to (messages of inbox whose id is theId)
    if (count of hits) > 0 then return item 1 of hits
    repeat with acct in accounts
      repeat with mb in mailboxes of acct
        set hits to (messages of mb whose id is theId)
        if (count of hits) > 0 then return item 1 of hits
      end repeat
    end repeat
    error "NOT_FOUND"
  end tell
end findMessage
`;

export function recipientLines(adresses: string, kind: "to" | "cc" | "bcc"): string {
  const list = adresses.split(/[,;]/).map((a) => a.trim()).filter((a) => a.length > 0);
  return list
    .map((a) => `    make new ${kind} recipient at end of ${kind} recipients with properties {address:${asStr(a)}}`)
    .join("\n");
}

export function recipientsBlock(opts: { to: string; cc?: string; cci?: string }): string {
  const toLines = recipientLines(opts.to, "to");
  if (!toLines) throw new MailError("Aucun destinataire valide fourni.");
  const parts = [toLines];
  if (opts.cc) { const l = recipientLines(opts.cc, "cc"); if (l) parts.push(l); }
  if (opts.cci) { const l = recipientLines(opts.cci, "bcc"); if (l) parts.push(l); }
  return parts.join("\n");
}

export function extraRecipientsBlock(cc?: string, cci?: string): string {
  const parts: string[] = [];
  if (cc) { const l = recipientLines(cc, "cc"); if (l) parts.push(l); }
  if (cci) { const l = recipientLines(cci, "bcc"); if (l) parts.push(l); }
  return parts.join("\n");
}

export function senderLine(adresse?: string): string {
  return adresse ? `  set sender of newMsg to ${asStr(adresse)}\n` : "";
}

export function buildListerScript(o: { filtre: "tous" | "non_lus"; expediteur?: string; limite: number }): string {
  const conditions: string[] = [];
  if (o.filtre === "non_lus") conditions.push("read status is false");
  if (o.expediteur) conditions.push(`sender contains ${asStr(o.expediteur)}`);
  const whose = conditions.length > 0 ? ` whose ${conditions.join(" and ")}` : "";
  return `
tell application "Mail"
  set FS to (character id 31)
  set RS to (character id 30)
  set theMessages to (messages of inbox${whose})
  set total to count of theMessages
  set maxN to ${o.limite}
  if total < maxN then set maxN to total
  set output to ""
  repeat with i from 1 to maxN
    set m to item i of theMessages
    set output to output & (id of m) & FS & (sender of m) & FS & (subject of m) & FS & ((date received of m) as string) & FS & (read status of m) & RS
  end repeat
  return output
end tell
`;
}

export function buildRechercherScript(o: { motCle: string; expediteur?: string; depuisJours?: number; inclureCorps: boolean; limite: number }): string {
  const kw = asStr(o.motCle);
  const corpsCheck = o.inclureCorps
    ? `    if (not matchFlag) then
      try
        if (content of m contains ${kw}) then set matchFlag to true
      end try
    end if`
    : "";
  const expediteurCheck = o.expediteur
    ? `    if matchFlag and (sender of m does not contain ${asStr(o.expediteur)}) then set matchFlag to false`
    : "";
  const dateSetup = o.depuisJours !== undefined ? `  set cutoff to (current date) - (${o.depuisJours} * days)` : "";
  const dateCheck = o.depuisJours !== undefined ? `    if matchFlag and ((date received of m) < cutoff) then set matchFlag to false` : "";
  return `
tell application "Mail"
  set FS to (character id 31)
  set RS to (character id 30)
${dateSetup}
  set theMessages to messages of inbox
  set total to count of theMessages
  set scanMax to 400
  if total < scanMax then set scanMax to total
  set maxN to ${o.limite}
  set output to ""
  set found to 0
  repeat with i from 1 to scanMax
    if found ≥ maxN then exit repeat
    set m to item i of theMessages
    set matchFlag to false
    if (subject of m contains ${kw}) then set matchFlag to true
    if (not matchFlag) and (sender of m contains ${kw}) then set matchFlag to true
${corpsCheck}
${expediteurCheck}
${dateCheck}
    if matchFlag then
      set output to output & (id of m) & FS & (sender of m) & FS & (subject of m) & FS & ((date received of m) as string) & FS & (read status of m) & RS
      set found to found + 1
    end if
  end repeat
  return output
end tell
`;
}

export function buildLireScript(id: number): string {
  return `${FIND_MESSAGE_HANDLER}
set m to findMessage(${id})
tell application "Mail"
  set FS to (character id 31)
  return "" & (id of m) & FS & (sender of m) & FS & (subject of m) & FS & ((date received of m) as string) & FS & (read status of m) & FS & (content of m)
end tell
`;
}

export function buildMarquerScript(id: number, lu: boolean): string {
  return `${FIND_MESSAGE_HANDLER}
set m to findMessage(${id})
tell application "Mail"
  set read status of m to ${lu ? "true" : "false"}
end tell
`;
}

export function buildDeplacerScript(id: number, dossier: string): string {
  return `${FIND_MESSAGE_HANDLER}
set m to findMessage(${id})
tell application "Mail"
  set targetMb to missing value
  repeat with acct in accounts
    try
      set targetMb to mailbox ${asStr(dossier)} of acct
    end try
  end repeat
  if targetMb is missing value then
    try
      set targetMb to mailbox ${asStr(dossier)}
    end try
  end if
  if targetMb is missing value then error "MAILBOX_NOT_FOUND"
  move m to targetMb
end tell
`;
}

export function buildCorbeilleScript(id: number): string {
  return `${FIND_MESSAGE_HANDLER}
set m to findMessage(${id})
tell application "Mail"
  delete m
end tell
`;
}
```

- [ ] **Step 5: Rebrancher `src/mail.ts` sur les builders et exporter `parseResumes`**

Dans `src/mail.ts` : supprimer `FIND_MESSAGE_HANDLER` et `recipientsBlock` locaux et l'import inline des templates ; importer depuis `scripts.js` ; remplacer chaque corps de fonction par un appel builder. Remplacer l'en-tête d'imports et les 8 fonctions existantes par :

```ts
import { runOsascript, FIELD_SEP, MailError } from "./applescript.js";
import {
  buildListerScript,
  buildRechercherScript,
  buildLireScript,
  buildMarquerScript,
  buildDeplacerScript,
  buildCorbeilleScript,
} from "./scripts.js";

export interface ResumeMail { id: number; expediteur: string; sujet: string; date: string; lu: boolean; }
export interface MailComplet extends ResumeMail { corps: string; }

export function parseResumes(raw: string): ResumeMail[] {
  if (!raw) return [];
  return raw
    .split(String.fromCharCode(30))
    .filter((r) => r.length > 0)
    .map((record) => {
      const [id, expediteur, sujet, date, lu] = record.split(FIELD_SEP);
      return { id: Number(id), expediteur: expediteur ?? "", sujet: sujet ?? "", date: date ?? "", lu: lu === "true" };
    });
}

export async function listerMails(opts: { filtre: "tous" | "non_lus"; expediteur?: string; limite: number }): Promise<ResumeMail[]> {
  return parseResumes(await runOsascript(buildListerScript(opts)));
}

export async function rechercherMails(opts: { motCle: string; expediteur?: string; depuisJours?: number; inclureCorps: boolean; limite: number }): Promise<ResumeMail[]> {
  return parseResumes(await runOsascript(buildRechercherScript(opts)));
}

export async function lireMail(id: number): Promise<MailComplet> {
  const raw = await runOsascript(buildLireScript(id));
  const [mid, expediteur, sujet, date, lu, ...corpsParts] = raw.split(FIELD_SEP);
  return { id: Number(mid), expediteur: expediteur ?? "", sujet: sujet ?? "", date: date ?? "", lu: lu === "true", corps: corpsParts.join(FIELD_SEP) };
}

export async function marquerMail(id: number, lu: boolean): Promise<void> {
  await runOsascript(buildMarquerScript(id, lu));
}

export async function deplacerMail(id: number, dossier: string): Promise<void> {
  await runOsascript(buildDeplacerScript(id, dossier));
}

export async function mettreCorbeille(id: number): Promise<void> {
  await runOsascript(buildCorbeilleScript(id));
}
```

> Note : `parseResumes` utilise `String.fromCharCode(30)` (RECORD_SEP) ; on garde l'import `FIELD_SEP` pour les champs. `creerBrouillon`/`envoyerMail` sont retirées ici et réintroduites en Task 4. `MailError` reste importé pour Task 3+. Si `tsc` signale `MailError` inutilisé à ce stade, retirer l'import temporairement puis le réajouter en Task 3.

- [ ] **Step 6: Lancer les tests pour vérifier le succès**

Run: `npm test`
Expected: PASS — les 12 tests de `scripts.test.ts` et `parsing.test.ts` passent ; `npm run build` compile sans erreur.

- [ ] **Step 7: Vérifier que les outils existants répondent toujours**

Run :
```bash
printf '%s\n' \
'{"jsonrpc":"2.0","id":1,"method":"initialize","params":{"protocolVersion":"2024-11-05","capabilities":{},"clientInfo":{"name":"t","version":"1.0"}}}' \
'{"jsonrpc":"2.0","method":"notifications/initialized"}' \
'{"jsonrpc":"2.0","id":2,"method":"tools/call","params":{"name":"lister_mails","arguments":{"filtre":"tous","limite":2}}}' \
| node dist/index.js 2>/dev/null | tail -1
```
Expected : un résultat JSON contenant 2 mails (comportement v1.0 intact).

- [ ] **Step 8: Commit**

```bash
git add src/scripts.ts src/mail.ts package.json test/
git commit -m "refactor: extraire les builders AppleScript dans scripts.ts + tests"
```

---

### Task 2: Outil `lister_comptes`

**Files:**
- Modify: `src/scripts.ts` (ajout `buildListerComptesScript`)
- Modify: `src/mail.ts` (ajout `Compte`, `parseComptes`, `listerComptes`)
- Modify: `src/index.ts` (enregistrement de l'outil)
- Test: `test/parsing.test.ts` (ajout)

**Interfaces:**
- Produces: `buildListerComptesScript(): string` ; `export interface Compte { nom: string; emails: string[] }` ; `parseComptes(raw: string): Compte[]` ; `listerComptes(): Promise<Compte[]>`.

- [ ] **Step 1: Écrire le test de `parseComptes` (échec attendu)**

Ajouter à `test/parsing.test.ts` :

```ts
import { parseComptes } from "../dist/mail.js";

test("parseComptes: un compte multi-adresses", () => {
  const raw = ["Perso", "a@x.com, a2@x.com"].join(FIELD_SEP) + RECORD_SEP
            + ["Pro", "b@y.com"].join(FIELD_SEP) + RECORD_SEP;
  const c = parseComptes(raw);
  assert.equal(c.length, 2);
  assert.deepEqual(c[0], { nom: "Perso", emails: ["a@x.com", "a2@x.com"] });
  assert.deepEqual(c[1], { nom: "Pro", emails: ["b@y.com"] });
});

test("parseComptes: vide -> tableau vide", () => {
  assert.deepEqual(parseComptes(""), []);
});
```

- [ ] **Step 2: Lancer le test pour vérifier l'échec**

Run: `npm test`
Expected: FAIL — `parseComptes` non exporté.

- [ ] **Step 3: Ajouter `buildListerComptesScript` à `src/scripts.ts`**

```ts
export function buildListerComptesScript(): string {
  return `
tell application "Mail"
  set FS to (character id 31)
  set RS to (character id 30)
  set output to ""
  repeat with acct in accounts
    set addrs to email addresses of acct
    set addrStr to ""
    repeat with a in addrs
      if addrStr is not "" then set addrStr to addrStr & ", "
      set addrStr to addrStr & a
    end repeat
    set output to output & (name of acct) & FS & addrStr & RS
  end repeat
  return output
end tell
`;
}
```

- [ ] **Step 4: Ajouter `Compte`, `parseComptes`, `listerComptes` à `src/mail.ts`**

```ts
import { buildListerComptesScript } from "./scripts.js"; // à fusionner avec l'import existant

export interface Compte { nom: string; emails: string[]; }

export function parseComptes(raw: string): Compte[] {
  if (!raw) return [];
  return raw
    .split(String.fromCharCode(30))
    .filter((r) => r.length > 0)
    .map((record) => {
      const [nom, addrCsv] = record.split(FIELD_SEP);
      const emails = (addrCsv ?? "").split(",").map((e) => e.trim()).filter((e) => e.length > 0);
      return { nom: nom ?? "", emails };
    });
}

export async function listerComptes(): Promise<Compte[]> {
  return parseComptes(await runOsascript(buildListerComptesScript()));
}
```

- [ ] **Step 5: Enregistrer l'outil dans `src/index.ts`**

Ajouter l'import `listerComptes` et, après le bloc Lecture :

```ts
server.registerTool(
  "lister_comptes",
  {
    title: "Lister les comptes",
    description:
      "Liste les comptes mail connectés à l'app Mail (nom et adresse(s) email). " +
      "Utile pour savoir depuis quelle adresse écrire avant de créer un brouillon ou d'envoyer.",
    inputSchema: {},
  },
  safe(async () => {
    const comptes = await listerComptes();
    if (comptes.length === 0) return texte("Aucun compte trouvé dans l'app Mail.");
    return texte(
      comptes.map((c) => `• ${c.nom} : ${c.emails.join(", ")}`).join("\n"),
    );
  }),
);
```

- [ ] **Step 6: Lancer les tests + vérifier l'outil**

Run: `npm test`
Expected: PASS (parsing comptes ok, build ok).

Run (réel) :
```bash
printf '%s\n' \
'{"jsonrpc":"2.0","id":1,"method":"initialize","params":{"protocolVersion":"2024-11-05","capabilities":{},"clientInfo":{"name":"t","version":"1.0"}}}' \
'{"jsonrpc":"2.0","method":"notifications/initialized"}' \
'{"jsonrpc":"2.0","id":2,"method":"tools/call","params":{"name":"lister_comptes","arguments":{}}}' \
| node dist/index.js 2>/dev/null | tail -1
```
Expected : la liste des comptes Mail réels (nom + adresses).

- [ ] **Step 7: Commit**

```bash
git add src/scripts.ts src/mail.ts src/index.ts test/
git commit -m "feat: outil lister_comptes"
```

---

### Task 3: Résolution de l'expéditeur + helpers CC/CCI (purs)

**Files:**
- Modify: `src/mail.ts` (ajout `resolveExpediteur`, `resolveExpediteurMaybe`)
- Test: `test/expediteur.test.ts`

**Interfaces:**
- Consumes: `Compte`, `listerComptes`, `MailError`.
- Produces: `resolveExpediteur(expediteur: string, comptes: Compte[]): string` (throw `MailError` si inconnu) ; `resolveExpediteurMaybe(expediteur?: string): Promise<string | undefined>`.

- [ ] **Step 1: Écrire les tests (échec attendu)**

Créer `test/expediteur.test.ts` :

```ts
import test from "node:test";
import assert from "node:assert";
import { resolveExpediteur } from "../dist/mail.js";

const comptes = [
  { nom: "Perso", emails: ["a@x.com", "a2@x.com"] },
  { nom: "Pro", emails: ["b@y.com"] },
];

test("resolveExpediteur: adresse connue (insensible à la casse) renvoie l'adresse", () => {
  assert.equal(resolveExpediteur("B@Y.com", comptes), "b@y.com");
});

test("resolveExpediteur: adresse secondaire d'un compte", () => {
  assert.equal(resolveExpediteur("a2@x.com", comptes), "a2@x.com");
});

test("resolveExpediteur: adresse inconnue lève une MailError listant les comptes", () => {
  assert.throws(() => resolveExpediteur("z@nope.com", comptes), /Comptes disponibles/);
});
```

- [ ] **Step 2: Lancer pour vérifier l'échec**

Run: `npm test`
Expected: FAIL — `resolveExpediteur` non exporté.

- [ ] **Step 3: Implémenter dans `src/mail.ts`**

```ts
export function resolveExpediteur(expediteur: string, comptes: Compte[]): string {
  const cible = expediteur.trim().toLowerCase();
  for (const c of comptes) {
    for (const e of c.emails) {
      if (e.trim().toLowerCase() === cible) return e.trim();
    }
  }
  const dispo = comptes.flatMap((c) => c.emails).join(", ");
  throw new MailError(`Adresse expéditrice « ${expediteur} » inconnue. Comptes disponibles : ${dispo || "(aucun)"}.`);
}

async function resolveExpediteurMaybe(expediteur?: string): Promise<string | undefined> {
  if (!expediteur) return undefined;
  return resolveExpediteur(expediteur, await listerComptes());
}
```

- [ ] **Step 4: Lancer pour vérifier le succès**

Run: `npm test`
Expected: PASS (3 nouveaux tests verts).

- [ ] **Step 5: Commit**

```bash
git add src/mail.ts test/expediteur.test.ts
git commit -m "feat: résolution et validation de l'adresse expéditrice"
```

---

### Task 4: Enrichir `creer_brouillon` / `envoyer_mail` (CC/CCI/expediteur)

**Files:**
- Modify: `src/scripts.ts` (ajout `buildComposeScript`)
- Modify: `src/mail.ts` (réintroduire `creerBrouillon`/`envoyerMail` avec options)
- Modify: `src/index.ts` (schémas enrichis)
- Test: `test/scripts.test.ts` (ajout)

**Interfaces:**
- Produces: `buildComposeScript(o)` ; `creerBrouillon(o)` / `envoyerMail(o)` acceptant `{ destinataire, sujet, corps, cc?, cci?, expediteur? }`.

- [ ] **Step 1: Écrire les tests de `buildComposeScript` (échec attendu)**

Ajouter à `test/scripts.test.ts` :

```ts
import { buildComposeScript } from "../dist/scripts.js";

test("buildComposeScript: save vs send", () => {
  const draft = buildComposeScript({ destinataire: "a@x.com", sujet: "S", corps: "B", action: "save" });
  assert.ok(draft.trimEnd().includes("save newMsg"));
  const sent = buildComposeScript({ destinataire: "a@x.com", sujet: "S", corps: "B", action: "send" });
  assert.ok(sent.trimEnd().includes("send newMsg"));
});

test("buildComposeScript: cc et cci ajoutent les bons recipients", () => {
  const s = buildComposeScript({ destinataire: "a@x.com", sujet: "S", corps: "B", cc: "c@x.com", cci: "d@x.com", action: "save" });
  assert.ok(s.includes("make new cc recipient"));
  assert.ok(s.includes('{address:"c@x.com"}'));
  assert.ok(s.includes("make new bcc recipient"));
  assert.ok(s.includes('{address:"d@x.com"}'));
});

test("buildComposeScript: expediteur pose le sender", () => {
  const s = buildComposeScript({ destinataire: "a@x.com", sujet: "S", corps: "B", expediteur: "me@x.com", action: "send" });
  assert.ok(s.includes('set sender of newMsg to "me@x.com"'));
});

test("buildComposeScript: sans expediteur ne pose pas de sender", () => {
  const s = buildComposeScript({ destinataire: "a@x.com", sujet: "S", corps: "B", action: "save" });
  assert.ok(!s.includes("set sender of newMsg"));
});
```

- [ ] **Step 2: Lancer pour vérifier l'échec**

Run: `npm test`
Expected: FAIL — `buildComposeScript` non exporté.

- [ ] **Step 3: Ajouter `buildComposeScript` à `src/scripts.ts`**

```ts
export function buildComposeScript(o: { destinataire: string; sujet: string; corps: string; cc?: string; cci?: string; expediteur?: string; action: "save" | "send" }): string {
  return `
tell application "Mail"
  set newMsg to make new outgoing message with properties {subject:${asStr(o.sujet)}, content:${asStr(o.corps)}, visible:false}
  tell newMsg
${recipientsBlock({ to: o.destinataire, cc: o.cc, cci: o.cci })}
  end tell
${senderLine(o.expediteur)}  ${o.action} newMsg
end tell
`;
}
```

- [ ] **Step 4: Réintroduire `creerBrouillon`/`envoyerMail` dans `src/mail.ts`**

```ts
import { buildComposeScript } from "./scripts.js"; // fusionner avec l'import existant

interface ComposeOpts { destinataire: string; sujet: string; corps: string; cc?: string; cci?: string; expediteur?: string; }

export async function creerBrouillon(o: ComposeOpts): Promise<void> {
  const expediteur = await resolveExpediteurMaybe(o.expediteur);
  await runOsascript(buildComposeScript({ ...o, expediteur, action: "save" }));
}

export async function envoyerMail(o: ComposeOpts): Promise<void> {
  const expediteur = await resolveExpediteurMaybe(o.expediteur);
  await runOsascript(buildComposeScript({ ...o, expediteur, action: "send" }));
}
```

- [ ] **Step 5: Enrichir les schémas dans `src/index.ts`**

Dans `creer_brouillon` ET `envoyer_mail`, ajouter au `inputSchema` (après `corps`) :

```ts
      cc: z.string().optional().describe("Adresse(s) en copie (CC), séparées par des virgules"),
      cci: z.string().optional().describe("Adresse(s) en copie cachée (CCI), séparées par des virgules"),
      expediteur: z.string().optional().describe("Adresse d'envoi (doit correspondre à un compte ; voir lister_comptes). Si absent, compte par défaut"),
```

Et adapter les handlers pour passer les nouveaux champs :

```ts
  safe(async ({ destinataire, sujet, corps, cc, cci, expediteur }) => {
    await creerBrouillon({ destinataire, sujet, corps, cc, cci, expediteur });
    return texte(`Brouillon créé pour ${destinataire} (sujet : « ${sujet} »). Il est dans tes Brouillons, non envoyé.`);
  }),
```

```ts
  safe(async ({ destinataire, sujet, corps, cc, cci, expediteur }) => {
    await envoyerMail({ destinataire, sujet, corps, cc, cci, expediteur });
    return texte(`Mail envoyé à ${destinataire} (sujet : « ${sujet} »).`);
  }),
```

- [ ] **Step 6: Lancer les tests + test réel d'un brouillon avec CC**

Run: `npm test`
Expected: PASS (4 nouveaux tests verts).

Run (réel, crée un brouillon de test) :
```bash
printf '%s\n' \
'{"jsonrpc":"2.0","id":1,"method":"initialize","params":{"protocolVersion":"2024-11-05","capabilities":{},"clientInfo":{"name":"t","version":"1.0"}}}' \
'{"jsonrpc":"2.0","method":"notifications/initialized"}' \
'{"jsonrpc":"2.0","id":2,"method":"tools/call","params":{"name":"creer_brouillon","arguments":{"destinataire":"test@example.com","sujet":"Test v1.1 CC - a supprimer","corps":"Corps","cc":"copie@example.com"}}}' \
| node dist/index.js 2>/dev/null | tail -1
```
Expected : message de succès ; vérifier dans Mail que le brouillon a bien un CC, puis le supprimer manuellement.

- [ ] **Step 7: Commit**

```bash
git add src/scripts.ts src/mail.ts src/index.ts test/
git commit -m "feat: CC/CCI et choix de l'expéditeur pour brouillon et envoi"
```

---

### Task 5: Outil `repondre_mail`

**Files:**
- Modify: `src/scripts.ts` (ajout `buildRepondreScript`)
- Modify: `src/mail.ts` (ajout `repondreMail`)
- Modify: `src/index.ts` (enregistrement de l'outil)
- Test: `test/scripts.test.ts` (ajout)

**Interfaces:**
- Produces: `buildRepondreScript(o)` ; `repondreMail(o: { id; corps; repondreATous; cc?; cci?; expediteur?; envoyer })`.

- [ ] **Step 1: Écrire les tests du builder (échec attendu)**

Ajouter à `test/scripts.test.ts` :

```ts
import { buildRepondreScript } from "../dist/scripts.js";

test("buildRepondreScript: utilise reply, préfixe le corps et respecte reply to all", () => {
  const s = buildRepondreScript({ id: 7, corps: "Bonjour", repondreATous: true, action: "save" });
  assert.ok(s.includes("findMessage(7)"));
  assert.ok(s.includes("reply m opening window false reply to all true"));
  assert.ok(s.includes('set content of newMsg to "Bonjour"'));
  assert.ok(s.trimEnd().includes("save newMsg"));
});

test("buildRepondreScript: envoyer -> send, et cc ajouté", () => {
  const s = buildRepondreScript({ id: 9, corps: "X", repondreATous: false, cc: "c@x.com", action: "send" });
  assert.ok(s.includes("reply to all false"));
  assert.ok(s.includes("make new cc recipient"));
  assert.ok(s.trimEnd().includes("send newMsg"));
});
```

- [ ] **Step 2: Lancer pour vérifier l'échec**

Run: `npm test`
Expected: FAIL — `buildRepondreScript` non exporté.

- [ ] **Step 3: Ajouter `buildRepondreScript` à `src/scripts.ts`**

```ts
export function buildRepondreScript(o: { id: number; corps: string; repondreATous: boolean; cc?: string; cci?: string; expediteur?: string; action: "save" | "send" }): string {
  const extra = extraRecipientsBlock(o.cc, o.cci);
  const extraBlock = extra ? `  tell newMsg\n${extra}\n  end tell\n` : "";
  return `${FIND_MESSAGE_HANDLER}
set m to findMessage(${o.id})
tell application "Mail"
  set newMsg to reply m opening window false reply to all ${o.repondreATous ? "true" : "false"}
  set content of newMsg to ${asStr(o.corps)} & linefeed & linefeed & (content of newMsg)
${extraBlock}${senderLine(o.expediteur)}  ${o.action} newMsg
end tell
`;
}
```

- [ ] **Step 4: Ajouter `repondreMail` à `src/mail.ts`**

```ts
import { buildRepondreScript } from "./scripts.js"; // fusionner avec l'import existant

export async function repondreMail(o: { id: number; corps: string; repondreATous: boolean; cc?: string; cci?: string; expediteur?: string; envoyer: boolean }): Promise<void> {
  const expediteur = await resolveExpediteurMaybe(o.expediteur);
  await runOsascript(buildRepondreScript({ ...o, expediteur, action: o.envoyer ? "send" : "save" }));
}
```

- [ ] **Step 5: Enregistrer l'outil dans `src/index.ts`**

Ajouter l'import `repondreMail` et, dans la section Rédaction :

```ts
server.registerTool(
  "repondre_mail",
  {
    title: "Répondre à un mail",
    description:
      "Répond à un mail (par son identifiant) en reprenant le fil, le destinataire et " +
      "l'objet « Re: ». Crée un BROUILLON par défaut (envoyer=false) ; mettre envoyer=true " +
      "pour envoyer directement. repondre_a_tous inclut tous les destinataires d'origine.",
    inputSchema: {
      id: z.number().int().describe("Identifiant du mail auquel répondre"),
      corps: z.string().describe("Texte de la réponse (ajouté au-dessus du message cité)"),
      repondre_a_tous: z.boolean().default(false).describe("Répondre à tous les destinataires. Défaut : false"),
      cc: z.string().optional().describe("Adresse(s) en copie (CC)"),
      cci: z.string().optional().describe("Adresse(s) en copie cachée (CCI)"),
      expediteur: z.string().optional().describe("Adresse d'envoi (voir lister_comptes). Si absent, Mail choisit le compte du mail d'origine"),
      envoyer: z.boolean().default(false).describe("true = envoyer ; false = créer un brouillon (défaut)"),
    },
  },
  safe(async ({ id, corps, repondre_a_tous, cc, cci, expediteur, envoyer }) => {
    await repondreMail({ id, corps, repondreATous: repondre_a_tous, cc, cci, expediteur, envoyer });
    return texte(envoyer ? `Réponse envoyée au mail ${id}.` : `Brouillon de réponse au mail ${id} créé (non envoyé).`);
  }),
);
```

- [ ] **Step 6: Lancer les tests**

Run: `npm test`
Expected: PASS (2 nouveaux tests verts).

- [ ] **Step 7: Vérification manuelle réelle (capricieux — à valider)**

Récupérer un id via `lister_mails` puis créer un brouillon de réponse :
```bash
printf '%s\n' \
'{"jsonrpc":"2.0","id":1,"method":"initialize","params":{"protocolVersion":"2024-11-05","capabilities":{},"clientInfo":{"name":"t","version":"1.0"}}}' \
'{"jsonrpc":"2.0","method":"notifications/initialized"}' \
'{"jsonrpc":"2.0","id":2,"method":"tools/call","params":{"name":"repondre_mail","arguments":{"id":<ID_REEL>,"corps":"Test reponse v1.1"}}}' \
| node dist/index.js 2>/dev/null | tail -1
```
Expected : un brouillon de réponse apparaît dans Mail, avec le bon destinataire, l'objet « Re: … », « Test reponse v1.1 » au-dessus du texte cité. Supprimer le brouillon ensuite.

> **Fallback si `reply` ne renvoie pas le message sortant** (le `set content`/`save` échoue) : remplacer `set newMsg to reply m ...` par `reply m opening window false reply to all <bool>` (sans capture) puis `set newMsg to outgoing message 1`. Re-tester. Documenter le choix retenu dans un commentaire de `buildRepondreScript`.

- [ ] **Step 8: Commit**

```bash
git add src/scripts.ts src/mail.ts src/index.ts test/
git commit -m "feat: outil repondre_mail (brouillon par défaut)"
```

---

### Task 6: Outil `transferer_mail`

**Files:**
- Modify: `src/scripts.ts` (ajout `buildTransfererScript`)
- Modify: `src/mail.ts` (ajout `transfererMail`)
- Modify: `src/index.ts` (enregistrement de l'outil)
- Test: `test/scripts.test.ts` (ajout)

**Interfaces:**
- Produces: `buildTransfererScript(o)` ; `transfererMail(o: { id; destinataire; corps?; cc?; cci?; expediteur?; envoyer })`.

- [ ] **Step 1: Écrire les tests du builder (échec attendu)**

Ajouter à `test/scripts.test.ts` :

```ts
import { buildTransfererScript } from "../dist/scripts.js";

test("buildTransfererScript: forward, destinataire en to, save par défaut", () => {
  const s = buildTransfererScript({ id: 12, destinataire: "dest@x.com", action: "save" });
  assert.ok(s.includes("findMessage(12)"));
  assert.ok(s.includes("forward m opening window false"));
  assert.ok(s.includes('make new to recipient at end of to recipients with properties {address:"dest@x.com"}'));
  assert.ok(s.trimEnd().includes("save newMsg"));
});

test("buildTransfererScript: corps optionnel préfixé seulement s'il est fourni", () => {
  const sans = buildTransfererScript({ id: 1, destinataire: "d@x.com", action: "save" });
  assert.ok(!sans.includes("set content of newMsg"));
  const avec = buildTransfererScript({ id: 1, destinataire: "d@x.com", corps: "Voir ci-dessous", action: "send" });
  assert.ok(avec.includes('set content of newMsg to "Voir ci-dessous"'));
  assert.ok(avec.trimEnd().includes("send newMsg"));
});
```

- [ ] **Step 2: Lancer pour vérifier l'échec**

Run: `npm test`
Expected: FAIL — `buildTransfererScript` non exporté.

- [ ] **Step 3: Ajouter `buildTransfererScript` à `src/scripts.ts`**

```ts
export function buildTransfererScript(o: { id: number; destinataire: string; corps?: string; cc?: string; cci?: string; expediteur?: string; action: "save" | "send" }): string {
  const corpsLine = o.corps ? `  set content of newMsg to ${asStr(o.corps)} & linefeed & linefeed & (content of newMsg)\n` : "";
  return `${FIND_MESSAGE_HANDLER}
set m to findMessage(${o.id})
tell application "Mail"
  set newMsg to forward m opening window false
  tell newMsg
${recipientsBlock({ to: o.destinataire, cc: o.cc, cci: o.cci })}
  end tell
${corpsLine}${senderLine(o.expediteur)}  ${o.action} newMsg
end tell
`;
}
```

- [ ] **Step 4: Ajouter `transfererMail` à `src/mail.ts`**

```ts
import { buildTransfererScript } from "./scripts.js"; // fusionner avec l'import existant

export async function transfererMail(o: { id: number; destinataire: string; corps?: string; cc?: string; cci?: string; expediteur?: string; envoyer: boolean }): Promise<void> {
  const expediteur = await resolveExpediteurMaybe(o.expediteur);
  await runOsascript(buildTransfererScript({ ...o, expediteur, action: o.envoyer ? "send" : "save" }));
}
```

- [ ] **Step 5: Enregistrer l'outil dans `src/index.ts`**

Ajouter l'import `transfererMail` et, dans la section Rédaction :

```ts
server.registerTool(
  "transferer_mail",
  {
    title: "Transférer un mail",
    description:
      "Transfère un mail (par son identifiant) vers un ou plusieurs destinataires. Crée un " +
      "BROUILLON par défaut (envoyer=false) ; mettre envoyer=true pour envoyer directement. " +
      "Le corps optionnel est ajouté en mot d'accompagnement au-dessus du message transféré.",
    inputSchema: {
      id: z.number().int().describe("Identifiant du mail à transférer"),
      destinataire: z.string().describe("Adresse(s) de destination, séparées par des virgules"),
      corps: z.string().optional().describe("Mot d'accompagnement optionnel (ajouté en haut)"),
      cc: z.string().optional().describe("Adresse(s) en copie (CC)"),
      cci: z.string().optional().describe("Adresse(s) en copie cachée (CCI)"),
      expediteur: z.string().optional().describe("Adresse d'envoi (voir lister_comptes). Si absent, compte par défaut"),
      envoyer: z.boolean().default(false).describe("true = envoyer ; false = créer un brouillon (défaut)"),
    },
  },
  safe(async ({ id, destinataire, corps, cc, cci, expediteur, envoyer }) => {
    await transfererMail({ id, destinataire, corps, cc, cci, expediteur, envoyer });
    return texte(envoyer ? `Mail ${id} transféré à ${destinataire}.` : `Brouillon de transfert du mail ${id} créé (non envoyé).`);
  }),
);
```

- [ ] **Step 6: Lancer les tests**

Run: `npm test`
Expected: PASS (2 nouveaux tests verts).

- [ ] **Step 7: Vérification manuelle réelle**

```bash
printf '%s\n' \
'{"jsonrpc":"2.0","id":1,"method":"initialize","params":{"protocolVersion":"2024-11-05","capabilities":{},"clientInfo":{"name":"t","version":"1.0"}}}' \
'{"jsonrpc":"2.0","method":"notifications/initialized"}' \
'{"jsonrpc":"2.0","id":2,"method":"tools/call","params":{"name":"transferer_mail","arguments":{"id":<ID_REEL>,"destinataire":"test@example.com","corps":"Pour info"}}}' \
| node dist/index.js 2>/dev/null | tail -1
```
Expected : un brouillon de transfert (objet « Fwd: … » ou « Tr: … ») avec le bon destinataire et « Pour info » en haut. Supprimer le brouillon ensuite.

> **Fallback** identique à Task 5 si `forward` ne renvoie pas le message sortant.

- [ ] **Step 8: Commit**

```bash
git add src/scripts.ts src/mail.ts src/index.ts test/
git commit -m "feat: outil transferer_mail (brouillon par défaut)"
```

---

### Task 7: Packaging et Release v1.1.0

**Files:**
- Modify: `manifest.json`, `package.json`, `README.md`

- [ ] **Step 1: Mettre à jour `manifest.json` (version + 10 outils)**

Passer `"version"` à `"1.1.0"` et remplacer le tableau `tools` par les 10 entrées :

```json
  "tools": [
    { "name": "lister_mails", "description": "Liste les derniers mails (filtre non lus / expéditeur)" },
    { "name": "rechercher_mails", "description": "Recherche dans les mails récents" },
    { "name": "lire_mail", "description": "Lit le contenu complet d'un mail" },
    { "name": "lister_comptes", "description": "Liste les comptes mail connectés" },
    { "name": "creer_brouillon", "description": "Crée un brouillon (CC/CCI/expéditeur)" },
    { "name": "envoyer_mail", "description": "Envoie un mail (CC/CCI/expéditeur)" },
    { "name": "repondre_mail", "description": "Répond à un mail (brouillon par défaut)" },
    { "name": "transferer_mail", "description": "Transfère un mail (brouillon par défaut)" },
    { "name": "marquer_mail", "description": "Marque un mail lu ou non lu" },
    { "name": "deplacer_mail", "description": "Déplace un mail vers un dossier" },
    { "name": "corbeille_mail", "description": "Met un mail à la corbeille (récupérable)" }
  ],
```

> Note : 11 entrées au total (les 8 d'origine + `lister_comptes` + `repondre_mail` + `transferer_mail`). Mettre aussi `corbeille_mail` — soit 11 lignes. Vérifier que toutes les lignes sont présentes.

- [ ] **Step 2: Mettre à jour `package.json`**

Passer `"version"` à `"1.1.0"`.

- [ ] **Step 3: Mettre à jour `README.md`**

Dans la table des outils, ajouter les lignes `lister_comptes`, `repondre_mail`, `transferer_mail` et mentionner CC/CCI/expéditeur sur brouillon/envoi. Ajouter un court paragraphe « Multi-comptes » expliquant `lister_comptes` + le paramètre `expediteur`.

- [ ] **Step 4: Tests + build complets**

Run: `npm test`
Expected: PASS (tous les tests).

Run :
```bash
printf '%s\n' \
'{"jsonrpc":"2.0","id":1,"method":"initialize","params":{"protocolVersion":"2024-11-05","capabilities":{},"clientInfo":{"name":"t","version":"1.0"}}}' \
'{"jsonrpc":"2.0","method":"notifications/initialized"}' \
'{"jsonrpc":"2.0","id":2,"method":"tools/list"}' \
| node dist/index.js 2>/dev/null | tail -1 | node -e 'let d="";process.stdin.on("data",c=>d+=c).on("end",()=>{console.log(JSON.parse(d).result.tools.length+" outils")})'
```
Expected : `11 outils`.

- [ ] **Step 5: Valider le manifeste et packager**

```bash
npx -y @anthropic-ai/mcpb validate manifest.json
npm prune --omit=dev
npx -y @anthropic-ai/mcpb pack . mail-macos.mcpb
npm install
```
Expected : « Manifest schema validation passes! » puis `mail-macos.mcpb` régénéré.

- [ ] **Step 6: Commit + Release GitHub v1.1.0**

```bash
git add manifest.json package.json package-lock.json README.md
git commit -m "release: v1.1.0 (multi-comptes, répondre/transférer, CC/CCI, tests)"
git push origin main
gh release create v1.1.0 mail-macos.mcpb --repo Maxtechexplore/mail-macos-mcp \
  --title "v1.1.0 — Multi-comptes, répondre/transférer, CC/CCI" \
  --notes "Ajoute lister_comptes, repondre_mail, transferer_mail, le CC/CCI et le choix de l'adresse expéditrice. Réinstaller le .mcpb dans Claude Desktop pour mettre à jour."
```
Expected : la release v1.1.0 est créée avec l'asset `mail-macos.mcpb`.

- [ ] **Step 7: Vérifier le lien stable**

Run: `curl -sIL -o /dev/null -w "%{http_code}\n" "https://github.com/Maxtechexplore/mail-macos-mcp/releases/latest/download/mail-macos.mcpb"`
Expected : `200`.

---

## Self-Review

**Couverture spec :**
- A (multi-comptes) → Task 2 (`lister_comptes`) + Task 3 (`resolveExpediteur`) + Task 4 (params `expediteur`). ✓
- B (répondre/transférer) → Task 5 + Task 6, brouillon par défaut. ✓
- C (CC/CCI) → helpers Task 1, intégration Tasks 4/5/6. ✓
- G (tests) → harness Task 1, tests ajoutés à chaque task. ✓
- Refactor `scripts.ts` → Task 1. ✓
- Livraison v1.1.0 → Task 7. ✓

**Placeholders :** aucun « TBD/TODO » ; tout le code est fourni. Les `<ID_REEL>` des vérifications manuelles sont des valeurs à substituer par l'exécutant (vérifs réelles, pas du code).

**Cohérence des types :** `Compte`, `ResumeMail`, `MailComplet` définis Task 1/2 ; `resolveExpediteur(expediteur, comptes)` signature stable Tasks 3→6 ; `action: "save"|"send"` cohérent dans tous les builders compose/repondre/transferer ; `envoyer: boolean` mappé vers `action` dans `mail.ts`.

**Risque connu :** `reply`/`forward` AppleScript (capture du message sortant). Fallback documenté Tasks 5/6, vérification manuelle obligatoire.
